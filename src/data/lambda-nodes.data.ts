import type { TopicNode } from '../components/FlowChart/flowchart.types';

const S = {
  concept: { label: '📖 What is it?',         bg: '#eff6ff' },
  example: { label: '🏗️ Real Example',         bg: '#f0fdf4' },
  story:   { label: '🍜 Pho24h Ghost Kitchen', bg: '#fff7ed' },
  examTip: { label: '🎯 SAA-C03 Exam Tip',     bg: '#fefce8' },
} as const;

const lambdaNodes: TopicNode[] = [
  {
    id: 'api-gw',
    icon: '🍽️',
    title: 'API Gateway',
    subtitle: 'Waiter who takes your order',
    color: 'coral',
    position: { x: 20, y: 3 },
    connections: ['lambda-fn'],
    sections: [
      { ...S.concept, content: 'API Gateway is a managed service that accepts HTTP/HTTPS requests and triggers Lambda functions. It handles routing, authentication, throttling, and CORS — then passes the request payload to Lambda and returns the response to the caller.' },
      { ...S.example, content: 'A React frontend calls POST /api/order to API Gateway. Gateway authenticates the JWT token, validates the request, and triggers the Lambda function. Lambda processes the order and saves it to DynamoDB. API Gateway returns the 200 response to the browser.' },
      { ...S.story,   content: '🍽️ The front-of-house waiter at Pho24h Ghost Kitchen. Customer sits down, makes an order. The waiter checks their membership card (auth), writes up the ticket in standard kitchen language (request transformation), and shouts it through the kitchen window. The waiter does NOT cook. The waiter does NOT deliver. The waiter handles the customer relationship so the ghost chef never has to talk to anyone directly.' },
      { ...S.examTip, content: '💡 SAA-C03: API Gateway + Lambda is the canonical serverless REST API pattern. Key settings: integration timeout (29s max for REST API), stage variables for env config, and Lambda authorizers for custom auth. Remember API Gateway charges per million requests — not per second.' },
    ],
  },
  {
    id: 'eventbridge',
    icon: '📅',
    title: 'EventBridge',
    subtitle: 'The alarm clock and event bus',
    color: 'purple',
    position: { x: 50, y: 3 },
    connections: ['lambda-fn'],
    sections: [
      { ...S.concept, content: 'EventBridge is a serverless event bus that routes events from AWS services, SaaS apps, and your own apps to Lambda and other targets. It supports scheduled rules (cron/rate) and event pattern matching for event-driven architectures.' },
      { ...S.example, content: 'Every night at 2am, an EventBridge cron rule triggers a Lambda that generates daily reports and saves them to S3. Another rule fires whenever an EC2 instance stops (event pattern), triggering a Lambda that pages the on-call engineer via SNS.' },
      { ...S.story,   content: '📅 The factory alarm system at Pho24h. "Every morning at 5am: wake the broth chef." "Every time the freezer temp drops: page maintenance." "Every time a VIP order arrives from the app: fast-track to the senior ghost." EventBridge is both the factory clock AND the intercom system. It coordinates every timed action and every reaction to something that just happened in the building.' },
      { ...S.examTip, content: '💡 SAA-C03: EventBridge (formerly CloudWatch Events) is the preferred way to schedule Lambda. Know the difference between EventBridge Scheduler (newer, more flexible, one-time or recurring) vs EventBridge Rules (event pattern matching + cron). Both can trigger Lambda.' },
    ],
  },
  {
    id: 'sqs',
    icon: '📬',
    title: 'SQS Queue',
    subtitle: 'The order ticket queue',
    color: 'amber',
    position: { x: 80, y: 3 },
    connections: ['lambda-fn'],
    sections: [
      { ...S.concept, content: 'SQS triggers Lambda via an event source mapping. Lambda polls the queue, reads messages in batches, and processes them. On failure, messages retry up to the queue\'s maxReceiveCount before going to the Dead Letter Queue. This decouples producers from consumers.' },
      { ...S.example, content: 'An e-commerce checkout service puts orders in SQS. Lambda processes them in batches of 10. If the payment API is temporarily down, orders pile up safely in SQS and Lambda retries them when the API recovers. No orders are lost, no manual intervention needed.' },
      { ...S.story,   content: '📬 The order ticket machine at Pho24h Ghost Kitchen. Every order becomes a physical ticket. The ghost chef watches the machine — when tickets pile up, it grabs a batch of 10 at once. If the kitchen explodes mid-batch, the tickets go BACK into the machine. After 3 failed attempts, a ticket graduates to the failed order basket (DLQ). The customer gets a refund email. Everyone is slightly sad.' },
      { ...S.examTip, content: '💡 SAA-C03: Lambda polls SQS (Lambda initiates, not SQS pushing). Batch size (1–10,000) and batch window tune throughput vs latency. SQS Standard = at-least-once delivery. SQS FIFO = exactly-once, ordered, requires a .fifo queue name. Lambda scales 1 concurrent instance per 60 messages for Standard queues.' },
    ],
  },
  {
    id: 'lambda-fn',
    icon: '⚡',
    title: 'Lambda Function',
    subtitle: 'The serverless ghost chef',
    color: 'amber',
    position: { x: 50, y: 18 },
    connections: ['exec-role', 'layers', 'env-vars', 'exec-env', 'destination', 'dlq'],
    sections: [
      { ...S.concept, content: 'A Lambda function is a piece of code that runs in response to an event. AWS manages the infrastructure, scaling, patching, and availability. You only pay for the milliseconds your code actually runs — there is no cost when idle. Lambda supports Node.js, Python, Java, Go, Ruby, .NET, and custom runtimes.' },
      { ...S.example, content: 'Airbnb uses Lambda to process booking events: when a booking is confirmed, one Lambda sends confirmation emails, another updates the host calendar, another charges the payment method — all triggered by a single EventBridge event. Each function scales independently.' },
      { ...S.story,   content: '⚡ The ghost chef of Pho24h. Does not exist when there are no orders. The moment an order arrives — POOF — ghost chef materializes in an empty kitchen, cooks the exact pho ordered, passes it to the delivery chute, and vanishes. No salary when idle. No break room. No health insurance. Order 1000 bowls simultaneously? 1000 ghost chefs appear simultaneously. Each in their own temporary kitchen. This is the magic of serverless.' },
      { ...S.examTip, content: '💡 SAA-C03: Know the hard limits: max execution time 15 minutes, memory 128MB–10,240MB, deployment package 50MB zipped / 250MB unzipped, /tmp storage up to 10GB. Lambda scales automatically up to the account concurrency limit (default 1,000). CPU scales proportionally with memory — not configurable separately.' },
    ],
  },
  {
    id: 'exec-role',
    icon: '🪪',
    title: 'Execution Role',
    subtitle: "Chef's security badge",
    color: 'gray',
    position: { x: 20, y: 33 },
    connections: [],
    sections: [
      { ...S.concept, content: 'An IAM Execution Role grants Lambda the permissions it needs to interact with other AWS services. Lambda assumes this role when it runs. Without the right permissions, Lambda cannot write to DynamoDB, read from S3, or publish to SNS.' },
      { ...S.example, content: 'A Lambda that resizes images needs: s3:GetObject (read source), s3:PutObject (write result), logs:CreateLogGroup and logs:PutLogEvents (write CloudWatch logs), and rekognition:DetectLabels (call Rekognition). Nothing else.' },
      { ...S.story,   content: '🪪 Every ghost chef at Pho24h wears a security badge. The badge says exactly what they can access: "Can enter cold storage. Cannot touch the cash register. Can use the industrial blender. Cannot open the owner\'s office safe." No badge = ghost is rejected at the front door. Wrong permissions = alarm goes off. Too many permissions = security audit nightmare and your boss yells at you.' },
      { ...S.examTip, content: '💡 SAA-C03: Lambda\'s trust policy must allow lambda.amazonaws.com to assume the role. The minimum for any Lambda function: AWSLambdaBasicExecutionRole (CloudWatch Logs). For VPC Lambda: also attach AWSLambdaVPCAccessExecutionRole. Always follow least privilege — never use AdministratorAccess on a Lambda role.' },
    ],
  },
  {
    id: 'layers',
    icon: '🧅',
    title: 'Lambda Layers',
    subtitle: 'Shared spice kit for all ghosts',
    color: 'teal',
    position: { x: 50, y: 33 },
    connections: [],
    sections: [
      { ...S.concept, content: 'A Lambda Layer is a ZIP archive containing shared libraries, dependencies, or custom runtimes that multiple Lambda functions can reference. Instead of bundling the same 50MB library in every function, you publish it as a layer and reference it from any function — keeping deployment packages small.' },
      { ...S.example, content: 'A team has 20 Lambda functions all requiring pandas, numpy, and an internal company SDK. They publish one layer with all three. All 20 functions reference the layer — deployment packages shrink from 80MB to under 2MB each. When they upgrade pandas, they publish a new layer version; functions using the old version are unaffected.' },
      { ...S.story,   content: '🧅 The shared spice cabinet at Pho24h Ghost Kitchen. Every ghost chef needs the same basics: star anise, fish sauce, MSG, the secret hoisin blend. Instead of each ghost materializing with their own personal spice bag (bloated 80MB package), the factory stocks one central cabinet (Lambda Layer). All 50 ghost chefs grab from the same cabinet. New spice added to the menu? Update the cabinet once. Every ghost benefits immediately.' },
      { ...S.examTip, content: '💡 SAA-C03: A function can reference up to 5 layers. Layers are versioned and immutable — publishing a new version does not break functions using the previous version. Layers can be shared across AWS accounts. The total unzipped size of function + all layers must stay under 250MB.' },
    ],
  },
  {
    id: 'env-vars',
    icon: '🔐',
    title: 'Environment Variables',
    subtitle: 'Secret recipe configuration',
    color: 'gray',
    position: { x: 80, y: 33 },
    connections: [],
    sections: [
      { ...S.concept, content: 'Environment variables let you configure Lambda functions without changing code. Database URLs, API keys, feature flags, and log levels are injected as key-value pairs at deploy time. Sensitive values are encrypted at rest with KMS.' },
      { ...S.example, content: 'The same Lambda code deploys to dev, staging, and prod. Only DB_URL, API_KEY, LOG_LEVEL, and FEATURE_FLAG_X differ per environment. No code changes needed between deployments. Secrets like API keys are encrypted with a customer KMS key so they never appear in plaintext in the AWS Console.' },
      { ...S.story,   content: '🔐 The recipe card slid under the ghost chef\'s kitchen door before each shift: "Today\'s secret broth ratio: 70% beef, 30% pork. Database password: [REDACTED]. Owner hotline: [KMS ENCRYPTED]." The chef reads it, memorizes it, cooks accordingly. The card never appears in the public recipe book (Git). The chef just follows the card. When the owner changes the ratio, they slide a new card — chef adapts instantly.' },
      { ...S.examTip, content: '💡 SAA-C03: Environment variables are encrypted at rest with KMS (AWS-managed key by default; use customer-managed keys for cross-account scenarios). For rotating secrets or complex config, use Secrets Manager or Parameter Store instead — and cache the value inside the Lambda execution environment to avoid per-invocation API calls.' },
    ],
  },
  {
    id: 'exec-env',
    icon: '🍳',
    title: 'Execution Environment',
    subtitle: 'The temporary ghost kitchen',
    color: 'blue',
    position: { x: 35, y: 48 },
    connections: ['memory-timeout', 'cold-start', 'concurrency'],
    sections: [
      { ...S.concept, content: 'When Lambda runs your function, it creates an isolated execution environment (a micro-VM using AWS Firecracker technology). This environment includes your code, the runtime, and any initialized resources. AWS reuses the same environment for subsequent invocations to improve performance — this is a "warm start."' },
      { ...S.example, content: 'A Node.js Lambda connects to RDS on first invocation (INIT phase). The database connection object is stored in the global scope. On the next 1,000 invocations, Lambda reuses the same warm environment — the DB connection is already open, cutting latency from 300ms to 8ms per call.' },
      { ...S.story,   content: '🍳 The temporary kitchen that materializes around each ghost chef. Full equipment, clean workspace, stocked fridge. AWS assembles it in milliseconds. First time the ghost appears: cold start — arrange tools, locate spices, preheat stove. After cooking, the kitchen STAYS assembled. Next order: INSTANT start, same warm kitchen. After enough idle time, the kitchen vanishes. The trick: store your DB connection handle outside the handler function — it persists across the warm kitchen\'s lifetime.' },
      { ...S.examTip, content: '💡 SAA-C03: Store heavy initializations (DB connections, SDK clients, config loading) OUTSIDE the handler function in the global/module scope. They persist across warm invocations and are initialized only once per execution environment. This is a classic Lambda performance optimization question.' },
    ],
  },
  {
    id: 'memory-timeout',
    icon: '⏱️',
    title: 'Memory & Timeout',
    subtitle: "Chef's capacity and time limit",
    color: 'blue',
    position: { x: 65, y: 48 },
    connections: [],
    sections: [
      { ...S.concept, content: 'Memory (128MB–10,240MB) controls how much RAM the function gets — and CPU power scales proportionally with memory. Timeout (1s–900s) is the maximum execution time allowed. If the function exceeds the timeout, Lambda forcibly terminates it and returns an error.' },
      { ...S.example, content: 'An image-resizing Lambda at 512MB runs in 3.2s. Bumping to 2,048MB (4x memory) cuts runtime to 0.7s (4x CPU). Total cost: 512MB × 3.2s vs 2,048MB × 0.7s — the higher-memory config is actually cheaper because GB-seconds are what you pay for.' },
      { ...S.story,   content: '⏱️ Every ghost chef gets two constraints: a kitchen of fixed size (memory) and a kitchen timer that ejects them when it rings (timeout). Tiny kitchen + short timer = cheap but risky. You try to cook a 12-course feast in a broom closet with a 30-second timer. The ghost who compresses video needs 10GB and 14 minutes. The ghost who validates an email needs 128MB and 0.5 seconds. Right-size your ghosts or waste money.' },
      { ...S.examTip, content: '💡 SAA-C03: CPU is NOT configured directly — it scales linearly with memory. At 1,769MB you get exactly 1 vCPU; at 3,538MB you get 2 vCPUs. Timeout max is 15 minutes — if your workload needs longer, use Step Functions + Lambda or AWS Batch instead. Always benchmark with AWS Lambda Power Tuning.' },
    ],
  },
  {
    id: 'cold-start',
    icon: '🥶',
    title: 'Cold Start',
    subtitle: 'The ghost waking up late',
    color: 'teal',
    position: { x: 20, y: 63 },
    connections: ['provisioned'],
    sections: [
      { ...S.concept, content: 'A cold start occurs when Lambda must create a new execution environment from scratch — downloading the deployment package, initializing the runtime, and running initialization code outside the handler. This adds latency ranging from ~100ms (Python/Node.js) to several seconds (Java with large classpaths).' },
      { ...S.example, content: 'A Java Lambda with Spring Boot cold-starts in ~3–5 seconds. Moving to GraalVM native image cuts cold start to ~200ms. Or switch to Provisioned Concurrency: zero cold starts guaranteed, at a fixed cost. For less critical workloads, scheduled warming pings (every 5 min via EventBridge) keep environments alive.' },
      { ...S.story,   content: "🥶 A ghost chef who hasn't worked in a while must go through the full morning routine: materialize from the spirit realm, put on uniform, find the kitchen, arrange utensils, preheat the stove, memorize the recipe card. This takes 1–5 seconds. The customer waits, slightly annoyed. After the first bowl, the chef is warm and snappy — next bowls take milliseconds. The horror: if the ghost is dismissed between orders, EVERY next customer gets the full morning routine." },
      { ...S.examTip, content: '💡 SAA-C03: Cold start mitigation: (1) Provisioned Concurrency — pre-warms N instances, eliminates cold starts, costs money at idle. (2) Keep deployment packages small. (3) Use lightweight runtimes (Python/Node.js vs Java). (4) Move heavy init outside the handler. VPC Lambda cold starts used to add 10+ seconds — now fast with Hyperplane ENI (since 2019).' },
    ],
  },
  {
    id: 'concurrency',
    icon: '👥',
    title: 'Concurrency',
    subtitle: 'How many ghost chefs at once',
    color: 'blue',
    position: { x: 50, y: 63 },
    connections: [],
    sections: [
      { ...S.concept, content: 'Concurrency is the number of Lambda function instances running simultaneously. Lambda auto-scales concurrency to match demand up to the account limit (default 1,000). Reserved Concurrency guarantees capacity for a specific function AND acts as a throttle cap.' },
      { ...S.example, content: 'An e-commerce platform reserves 500 concurrent executions for the checkout Lambda on Black Friday. Even if other Lambdas exhaust the account limit, checkout always has 500 slots. A batch job is capped at 10 concurrent instances so it cannot starve the rest of the system.' },
      { ...S.story,   content: '👥 When 500 customers order pho simultaneously, 500 ghost chefs materialize at once, each in their own temporary kitchen. The whole factory expands to fit them. The account-level ghost capacity is 1,000. You can "reserve" 500 slots for the premium counter (reserved concurrency) — they can never be stolen by the discount counter. Without a reservation, the discount ghosts could hog every slot during a lunch rush and the premium counter starves. That would be a tragedy.' },
      { ...S.examTip, content: '💡 SAA-C03: Two types to know: Reserved Concurrency (hard cap for a function — also guarantees minimum available capacity for it) vs Provisioned Concurrency (pre-initialized warm instances — eliminates cold starts). They are independent settings. Reserved = 0 still works but means the function can be throttled to zero.' },
    ],
  },
  {
    id: 'provisioned',
    icon: '🔥',
    title: 'Provisioned Concurrency',
    subtitle: 'Pre-warmed chefs on standby',
    color: 'amber',
    position: { x: 80, y: 63 },
    connections: [],
    sections: [
      { ...S.concept, content: 'Provisioned Concurrency pre-initializes a specified number of Lambda execution environments so they are always ready to respond instantly — eliminating cold starts completely. You pay for the pre-warmed instances even when they receive zero invocations.' },
      { ...S.example, content: 'A financial trading platform\'s order Lambda must respond in under 50ms. They configure 100 provisioned concurrent instances. Every invocation hits a pre-warmed environment — guaranteed sub-10ms handler latency. AWS Auto Scaling scales provisioned concurrency up before market open and down after close.' },
      { ...S.story,   content: '🔥 The VIP ghost chef program. Instead of waiting for a ghost to materialize from nothing, Pho24h pays to keep 100 chefs fully dressed, standing in their kitchens, stoves pre-heated, staring at the order window doing absolutely nothing. Waiting. Ready. The moment any order arrives: INSTANT response. Zero morning routine. You pay these 100 ghosts their standby salary even if they cook zero bowls per day. Expensive. But for VIP customers who refuse to wait even one second, it is worth every dong.' },
      { ...S.examTip, content: '💡 SAA-C03: Provisioned Concurrency eliminates cold starts but charges for pre-initialized instances even at zero traffic. Pair it with Application Auto Scaling to scale it up before a traffic surge (e.g., before 9am market open) and down afterward. Must be applied to a Lambda alias or a specific version — not $LATEST.' },
    ],
  },
  {
    id: 'dlq',
    icon: '💀',
    title: 'Dead Letter Queue',
    subtitle: 'The failed order graveyard',
    color: 'red',
    position: { x: 30, y: 78 },
    connections: [],
    sections: [
      { ...S.concept, content: 'A Dead Letter Queue captures events that Lambda failed to process after all retry attempts. For async invocations, Lambda retries twice by default. After all retries, the failing event is sent to the configured SQS queue or SNS topic (the DLQ), preventing silent data loss.' },
      { ...S.example, content: 'Lambda processes payment confirmation events from SNS. One message has a malformed JSON payload — Lambda crashes on it 3 times. The message lands in the DLQ SQS queue. An engineer gets a CloudWatch alarm, inspects the DLQ message, fixes the upstream bug that sent bad JSON, and re-queues the corrected message.' },
      { ...S.story,   content: '💀 The sad basket of failed orders at Pho24h Ghost Kitchen. The ghost chef tried 3 times to fulfill this ticket. Attempt 1: kitchen caught fire. Attempt 2: the order was illegible (bad JSON). Attempt 3: ghost had an existential crisis. Ticket goes to the failed order basket (DLQ). An ops manager reviews it later, figures out what went wrong, fixes it, tries again. Without the DLQ, the ticket disappears forever and nobody ever finds out.' },
      { ...S.examTip, content: '💡 SAA-C03: DLQ applies to ASYNC invocations (SNS, EventBridge, S3 events, async SDK calls). For SYNC invocations (API Gateway, direct Lambda invoke), errors return directly to the caller — no DLQ involved. Lambda Destinations are a newer, more flexible alternative that handles both success and failure routing.' },
    ],
  },
  {
    id: 'destination',
    icon: '📦',
    title: 'Lambda Destination',
    subtitle: 'Where the finished pho gets sent',
    color: 'green',
    position: { x: 70, y: 78 },
    connections: [],
    sections: [
      { ...S.concept, content: 'Lambda Destinations route the result of an async invocation to another AWS service — on success OR on failure. Supported targets: SQS, SNS, EventBridge, and another Lambda function. Unlike DLQ (failure only), Destinations handle both outcomes and carry the full event payload plus function response.' },
      { ...S.example, content: 'An image-processing Lambda sends successful results to an SNS topic (user gets an email: "Your photo is ready!") and failed results to an SQS queue for retry processing. Both success and failure paths are wired up without a single extra line of code in the Lambda function.' },
      { ...S.story,   content: '📦 The Pho24h delivery chute system. After the ghost chef finishes cooking, they push the bowl into a smart chute. Perfect pho: chute A → VIP dining room (success destination). Disaster pho: chute B → kitchen manager\'s complaint desk (failure destination). The chef never decides where the bowl goes — they just cook. The chute routes it. This is cleaner than the old DLQ system where the chef personally carried failed pho to the graveyard. The chef should never have to know about the graveyard.' },
      { ...S.examTip, content: '💡 SAA-C03: Lambda Destinations are more powerful than DLQ: they capture the full invocation record (event + response + metadata), they handle success AND failure (DLQ handles failure only), and they work with EventBridge for fan-out scenarios. Use Destinations for new designs; DLQ remains valid for simpler failure-only routing.' },
    ],
  },
];

export default lambdaNodes;
